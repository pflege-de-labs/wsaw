#!/bin/bash
echo "[stub fallocate] $*" >&2
# user_data.sh chmod/mkswap/swapon's the path afterwards; a real path
# without a real fallocate would break those on a plain "file not found".
touch "${@: -1}"
exit 0
